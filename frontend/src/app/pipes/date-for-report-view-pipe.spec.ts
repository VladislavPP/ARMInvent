import { DateForReportViewPipe } from './date-for-report-view-pipe';

describe('DateForReportViewPipe', () => {
  it('create an instance', () => {
    const pipe = new DateForReportViewPipe();
    expect(pipe).toBeTruthy();
  });
});
