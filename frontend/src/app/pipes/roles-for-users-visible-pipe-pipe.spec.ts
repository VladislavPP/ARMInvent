import { RolesForUsersVisiblePipePipe } from './roles-for-users-visible-pipe-pipe';

describe('RolesForUsersVisiblePipePipe', () => {
  it('create an instance', () => {
    const pipe = new RolesForUsersVisiblePipePipe();
    expect(pipe).toBeTruthy();
  });
});
