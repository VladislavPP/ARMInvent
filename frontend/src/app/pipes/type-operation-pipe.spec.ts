import { TypeOperationPipe } from './type-operation-pipe';

describe('TypeOperationPipe', () => {
  it('create an instance', () => {
    const pipe = new TypeOperationPipe();
    expect(pipe).toBeTruthy();
  });
});
