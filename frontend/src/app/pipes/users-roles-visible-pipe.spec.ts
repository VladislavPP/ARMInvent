import { UsersRolesVisiblePipe } from './users-roles-visible-pipe';

describe('UsersRolesVisiblePipe', () => {
  it('create an instance', () => {
    const pipe = new UsersRolesVisiblePipe();
    expect(pipe).toBeTruthy();
  });
});
